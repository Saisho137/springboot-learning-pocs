spring:
  application:
    name: demo
  # Deshabilitamos el import de Parameter Store en tests
  config:
    import: "optional:aws-parameterstore:/demo/"

# Deshabilitamos la resolución automática de Parameter Store para tests
spring.cloud.aws.parameterstore:
  enabled: false

aws:
  region: us-east-1
  credentials:
    access-key: test-access-key
    secret-key: test-secret-key
    profile-name: test
    token: test-token
  parameter-store:
    enabled: false
    prefix: /demo/

# Valores mock para tests
app:
  name: test-name
  description: test-description

# También deshabilitamos las propiedades de name y description para tests
name: test-name
description: test-description

